{
  "routes": [
    {
      "method": "GET",
      "path": "/contact-emails",
      "handler": "contact-email.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-emails/count",
      "handler": "contact-email.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-emails/:id",
      "handler": "contact-email.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contact-emails",
      "handler": "contact-email.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contact-emails/:id",
      "handler": "contact-email.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contact-emails/:id",
      "handler": "contact-email.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
